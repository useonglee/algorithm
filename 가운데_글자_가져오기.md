```js
function solution(s) {
  let answer = "";
  let midIdx = 0;

  if (s.length % 2 === 1) {
    midIdx = (s.length - 1) / 2;
    answer = s[midIdx];
  } else {
    midIdx = s.length / 2;
    answer = s[midIdx - 1] + s[midIdx];
  }
  return answer;
}
```
